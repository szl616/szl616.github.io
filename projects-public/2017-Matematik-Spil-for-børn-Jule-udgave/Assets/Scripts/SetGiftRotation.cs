using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetGiftRotation : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.rotation = Quaternion.Euler(new Vector3(-14.478f, -63.435f, 26.565f));
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
